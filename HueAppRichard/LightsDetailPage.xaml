<Page
    x:Class="HueAppRichard.LightsDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueAppRichard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <TextBlock Name="lightName" Text="{Binding name}" HorizontalAlignment="Center"></TextBlock>
            <TextBlock Name="lightType" Text="{Binding type}" HorizontalAlignment="Center"></TextBlock>
            <TextBlock Name="lightId" Text="{Binding id}" HorizontalAlignment="Center"></TextBlock>
            
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Brightness" HorizontalAlignment="Left"></TextBlock>
                <TextBlock Name="brightnessValue" Text="{Binding brightness}" Margin="250, 0, 0, 0"></TextBlock>
            </StackPanel>
            <Slider Name="brightnessSlider" Maximum="254" Value="{Binding brightness}" ValueChanged="brightnessSlider_ValueChanged"></Slider>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Saturation" HorizontalAlignment="Left"></TextBlock>
                <TextBlock Name="saturationValue" Text="{Binding saturation}" Margin="250, 0, 0, 0"></TextBlock>
            </StackPanel>
            <Slider Name="saturationSlider" Maximum="254" Value="{Binding saturation}" ValueChanged="saturationSlider_ValueChanged"></Slider>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Hue" HorizontalAlignment="Left"></TextBlock>
                <TextBlock Name="hueValue" Text="{Binding hue}" Margin="250, 0, 0, 0"></TextBlock>
            </StackPanel>
            <Slider Name="hueSlider" Maximum="65280" Value="{Binding hue}" ValueChanged="hueSlider_ValueChanged"></Slider>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Colorloop" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                <ToggleSwitch Name="colorloop" Margin="50, 0, 0, 0" IsOn="{Binding effect}" Toggled="colorloop_Toggled"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="On/Off" VerticalAlignment="Center" HorizontalAlignment="Left"></TextBlock>
                <ToggleSwitch Name="isOn" Margin="50, 0, 0, 0" IsOn="{Binding isOn}" Toggled="isOn_Toggled"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
